import React from "react";

const AddNote = () => {
  return (
    <div>
      <form action="">
        <input type="text" />
        <button>Add Note</button>
      </form>
    </div>
  );
};

export default AddNote;
